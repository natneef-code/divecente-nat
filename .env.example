# No credentials or environment variables are needed in browser demo mode.
# Future server-only integrations: DATABASE_URL, AUTH_SECRET, PAYMENT_WEBHOOK_SECRET.
# Never put a secret in a VITE_ variable: those values are public browser code.
